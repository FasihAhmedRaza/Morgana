import CancelComponent from "@/components/PaymentCancel";

const page = () => {
  return (
    <CancelComponent message="We're sorry for the inconvenience. You can try making the payment again." />
  );
};

export default page;
